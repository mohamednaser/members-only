<h1>New Session</h1>

<%= form_for( :session , ) do |f| %>
    <%= f.label (:email) %>
    <%= f.text_field (:email) %>

    <%= f.label (:password) %>
    <%= f.password_field (:password) %>

    <%= f.submit "Login", disable_with: 'Submiting...' %>
<% end %>
